# SCROLL ISSUE FIX PLAN ✅

## Step 1: Create TODO-animations.md (Done)

- [x] Document Lenis + ScrollTrigger sync plan

## Step 2: Edit js/scripts.js \n- [x] Added ScrollTrigger + Lenis sync (scrollerProxy + RAF) ✅

## Step 3: Test

- [ ] Open index.html → Verify smooth scroll + animations work
- [ ] Test archive reel pinning + card reveals
- [ ] Test cursor follow + service previews

## Step 4: Complete

- [ ] attempt_completion()

**Next: Edit js/scripts.js**
# 1. Initialize git if needed
git init

# 2. Add your new GitHub repo as remote (replace URL)
git remote add origin https://github.com/your-username/your-repo.git

# 3. Add all files and commit
git add .
git commit -m "Initial commit"

# 4. Push to the new repo (main branch)
git push -u origin main# 1. Initialize git if needed
git init

# 2. Add your new GitHub repo as remote (replace URL)
git remote add origin https://github.com/your-username/your-repo.git

# 3. Add all files and commit
git add .
git commit -m "Initial commit"

# 4. Push to the new repo (main branch)
git push -u origin main